spring:

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  flyway:
    enabled: false

  main:
    allow-bean-definition-overriding: true

security:
    jwt:
        secret: test-secret
        expiration: 300000
        refresh-expiration: 900000


